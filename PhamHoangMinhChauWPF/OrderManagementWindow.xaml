<Window x:Class="PhamHoangMinhChauWPF.OrderManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Order Management" Height="400" Width="592">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Order Management"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <ListView Grid.Row="1"
          x:Name="OrdersListView"
          Margin="0,0,0,10"
          SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Product Name" Width="200" DisplayMemberBinding="{Binding ProductName}" />
                    <GridViewColumn Header="Quantity" Width="80" DisplayMemberBinding="{Binding Quantity}" />
                    <GridViewColumn Header="Price" Width="100" DisplayMemberBinding="{Binding UnitPrice}" />
                    <GridViewColumn Header="Discount" Width="100" DisplayMemberBinding="{Binding Discount}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnAdd" Content="Add" Width="100" Margin="5" Click="btnAdd_Click"/>
            <Button x:Name="btnDelete" Content="Delete" Width="80" Margin="5"/>
            <Button x:Name="btnUpdate" Content="Update" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
